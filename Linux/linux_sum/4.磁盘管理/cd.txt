cd
    命令用于切换当前工作目录至 dirName(目录参数),dirName 表示法可为绝对路径或相对路径。若目
    录名称省略，则变换至使用者的 home 目录."~" 也表示为 home 目录 的意思，"." 则是表示目前
    所在的目录，".." 则表示目前目录位置的上一层目录

dirs
    显示目录记录
    dirs [+/-n -l]
    
    参数：
        +n 显示从左边算起第n笔的目录。
        -n 显示从右边算起第n笔的目录。
        -l 显示目录完整的记录。
