history 命令
    位置  .bash_history
    选项
        -a  追加  -d #  删除指定命令   -c:  清除所有命令
    调用历史命令
        !#: 重新执行历史中编号为#的命令
        !str: 再次执行最近一次以 str 开头的命令
        !?str: 再次执行最近一次含有 str 的命令
        !!: 重新执行最近的一次命令
        !$: 表示上一条命令参数
